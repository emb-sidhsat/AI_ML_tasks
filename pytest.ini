[pytest]
testpaths = tests
addopts   = -ra --tb=short
markers =
    unit: fast unit test with no external dependencies
    integration: requires live NHTSA API or large model downloads
    slow: takes more than 5 seconds (SBERT / BART)
